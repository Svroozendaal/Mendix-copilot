# Mendix Personal Access Token
# Maak aan via: https://user-settings.mendixcloud.com/link/developersettings
# Vereiste scopes: mx:modelrepository:repo:read, mx:modelrepository:repo:write
MENDIX_TOKEN=

# Mendix App ID (te vinden op de General tab in het Developer Portal)
MENDIX_APP_ID=

# Branch naam (standaard: main)
MENDIX_BRANCH=main

# Local copilot-api host/port (optioneel)
COPILOT_API_HOST=127.0.0.1
COPILOT_API_PORT=8787

# Frontend API base URL (alleen voor web-ui/Vite)
VITE_API_BASE_URL=http://127.0.0.1:8787

# Chat safety timeouts (optioneel, milliseconden)
COPILOT_CHAT_STEP_TIMEOUT_MS=120000
COPILOT_CHAT_TOTAL_TIMEOUT_MS=240000

# Optionele shared secret voor /api/plan/execute
COPILOT_APPROVAL_TOKEN=
