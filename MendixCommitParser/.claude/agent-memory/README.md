# Commit Parser Agent - Memory System

This directory will store:
- Session state when the agent is running
- Learned patterns from training data
- Configuration (for example folder paths and thresholds)

Currently empty - it will be populated when the agent is trained in a future phase.
